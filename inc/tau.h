#pragma once

// provide the tau_t data type and tau global variable
// that represent the current simulated time, which is
// available to all code within the simulation.

typedef unsigned long tau_t;
extern tau_t TAU;
